import React from 'react';
import UserHeader from './UserHeader.jsx';
import MenuHeader from './MenuHeader.jsx';

export default () => {
    return (
        <div>
            <UserHeader />
            <MenuHeader />
        </div>
    );
}